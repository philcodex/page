source "https://rubygems.org"

# Use the github-pages gem to match GitHub's environment
gem "github-pages", group: :jekyll_plugins

# Explicitly add the theme so it works locally
gem "minima", "~> 2.5"

# Necessary for Ruby 3.x support
gem "webrick", "~> 1.7"
